{% extends 'base.html.twig' %}

{% block title %}Client{% endblock %}

{% block body %}
    <h1>Client</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ client.id }}</td>
            </tr>
            <tr>
                <th>NameOfSociety</th>
                <td>{{ client.NameOfSociety }}</td>
            </tr>
            <tr>
                <th>ContactName</th>
                <td>{{ client.contactName }}</td>
            </tr>
            <tr>
                <th>ContactPosition</th>
                <td>{{ client.contactPosition }}</td>
            </tr>
            <tr>
                <th>PhoneContact</th>
                <td>{{ client.phoneContact }}</td>
            </tr>
            <tr>
                <th>EmailContact</th>
                <td>{{ client.emailContact }}</td>
            </tr>
            <tr>
                <th>Notes</th>
                <td>{{ client.notes }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('client_index') }}">back to list</a>

    <a href="{{ path('client_edit', {'id': client.id}) }}">edit</a>

    {{ include('client/_delete_form.html.twig') }}
{% endblock %}
